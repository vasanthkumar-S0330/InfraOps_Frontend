# Replace 'YOUR_AWS_PUBLIC_IP' with the actual public IP address or domain name of your AWS instance.
# Do NOT use 'localhost' here, because the frontend runs in the user's browser, and 'localhost' would point to the user's personal computer instead of your AWS server.
VITE_API_URL=http://3.109.124.170:8081/api
